<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>frugali-TEA</title>
</head>
<body>
  <form id="signup" action="/signup" method="POST">
    <label for="name"></label>
    <input type="text" placeholder="First Name*" name="firstname" value="<%=typeof name != 'undefined' ? name : '' %>" autocomplete="off" required>
    <input type="text" placeholder="Last Name*" name="lastname" value="<%=typeof lastname != 'undefined' ? lastname : '' %>" autocomplete="off" required><br>
    <input type="text" placeholder="Create a Username*" name="username" value="<%=typeof username != 'undefined' ? username : '' %>" autocomplete="off" required>
    <input type="text" name="email" placeholder="Email Address" value="<%=typeof email != 'undefined' ? email : '' %>" autocomplete="off" required><br>
    <input type="password" name="password" placeholder="Set a password" value="<%=typeof password != 'undefined' ? passowrd : '' %>" autocomplete="off" required>
    <input type="password" name="confirmPassword" placeholder="Confirm password" value="<%=typeof confirmPassword != 'undefined' ? confirmPassword : '' %>" autocomplete="off" required><br>
    <button type="submit">Submit</button>
  </form>
</body>
</html>