<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" type="text/css" href="static/css/signup.css">
  <link rel="stylesheet" type="text/css" href="static/css/signin.css">
  <link rel="stylesheet" type="text/css" href="static/css/homepage.css">
  <link rel="stylesheet" type="text/css" href="static/css/navbar.css">
  <title>frugali-TEA</title>
  <script>
    function getval() {
      alert("Tea is good for you. It takes around 2,000 tiny leaves to make just one pound of finished tea. ....GENERAL RULES .Do not leave the kettle alone when boiling tea. ...Do not brew the tea for fewer than 3 minutes. ...Do not leave the teabag in the mug. ...Do not ever use any of these (unless life or death). .NEVER put the sugar teaspoon into the tea. ...Never use a different type of milk than anticipated. ...Never ever EVER pour the milk in first");
    }
  </script>
</head>
<body>
  <nav>
      <ul class="topnav">
      <li><a href='signin'>Sign In</a></li>
      <li><a href='About Us'>About Us</a></li>
      <li><a href='Contact Us'>Contact Us</a></li>
    </ul>
  </nav>
  <%- include ('./partials/messages') %>
  <div class="container">
    <form id="signup" action="/signup" method="POST">
      <h1 class="header1">Sign up</h1>
      <p>Please fill in this form to create an account.</p>
      <label for="name"></label>
      <input type="text" placeholder="First Name*" name="firstname" value="<%=typeof name != 'undefined' ? name : '' %>" class="form-input" autocomplete="off" required>
      <input type="text" placeholder="Last Name*" name="lastname" value="<%=typeof lastname != 'undefined' ? lastname : '' %>" class="form-input" autocomplete="off" required><br>
      <input type="text" placeholder="Create a Username*" name="username" value="<%=typeof username != 'undefined' ? username : '' %>" class="form-input" autocomplete="off" required>
      <input type="text" name="email" placeholder="Email Address" value="<%=typeof email != 'undefined' ? email : '' %>" class="form-input" autocomplete="off" required><br>
      <input type="password" name="password" placeholder="Set a password" value="<%=typeof password != 'undefined' ? password : '' %>" class="form-input" autocomplete="off" required>
      <input type="password" name="confirmPassword" placeholder="Confirm password" value="<%=typeof confirmPassword != 'undefined' ? confirmPassword : '' %>" class="form-input" autocomplete="off" required><br>
      <button type="submit" class="button bouncy">Submit</button>
      <input type='reset' class="button bouncy" value='Reset' name='reset'>
      <p>By creating an account you agree to our <button type ="button" class="button bouncy" onclick="getval()">Terms & Condition</button>.</p>
    </form>
  </div>
</div>
</body>
</html>
